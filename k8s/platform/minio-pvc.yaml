apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  namespace: platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: datanauts-durable
  volumeName: platform-minio-pv
  resources:
    requests:
      storage: 20Gi
