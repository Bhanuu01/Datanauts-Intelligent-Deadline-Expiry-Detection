apiVersion: v1
kind: Service
metadata:
  name: deadline-inference-canary
  namespace: ml
spec:
  selector:
    app: deadline-inference-canary
  ports:
    - name: http
      port: 8001
      targetPort: 8001
