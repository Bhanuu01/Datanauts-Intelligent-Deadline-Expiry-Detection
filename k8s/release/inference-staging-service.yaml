apiVersion: v1
kind: Service
metadata:
  name: deadline-onnx-serving-staging
  namespace: ml
spec:
  selector:
    app: deadline-onnx-serving-staging
  ports:
    - name: http
      port: 8004
      targetPort: 8004
