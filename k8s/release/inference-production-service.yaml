apiVersion: v1
kind: Service
metadata:
  name: deadline-inference-production
  namespace: ml
spec:
  selector:
    app: deadline-inference-production
  ports:
    - name: http
      port: 8001
      targetPort: 8001
