apiVersion: v1
kind: Service
metadata:
  name: deadline-onnx-serving-production
  namespace: ml
spec:
  selector:
    app: deadline-onnx-serving-production
  ports:
    - name: http
      port: 8004
      targetPort: 8004
