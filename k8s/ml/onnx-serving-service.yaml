apiVersion: v1
kind: Service
metadata:
  name: deadline-onnx-serving
  namespace: ml
spec:
  selector:
    app: deadline-onnx-serving
  ports:
    - protocol: TCP
      port: 8004
      targetPort: 8004
